#include <stdio.h>

void test()
{
	//static 修饰局部变量
	//局部变量的生命周期变长
	static int a = 1;//a是一个静态的局部变量
	a++;
	printf("a = %d\n", a);
}

int main()
{
	
	int i = 0;
	while (i<5)
	{
		test();
		i++;
	}

return 0;

}
